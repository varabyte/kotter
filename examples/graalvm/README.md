A simple example which supports building native images using the Gradle plugin for [GraalVM Native Image](https://graalvm.github.io/native-build-tools/latest/index.html).

Build executable: `gradle nativeCompile`
Run executable: `./build/native/nativeCompile/graalvm`
